1、正则表达式匹配
    '00\d' 可以匹配 001,002   。。。

2、RE模块
    re.match(string[,pos[,endpos]]):

3、logging日志模块 
    import logging
    日志等级：使用范围（高到低
        FATAL：致命错误
        CRITICAL:特别糟糕的事情，如内存耗尽，磁盘空间为空，一般很少使用
        ERROR：发生错误时，如IO操作失败或连接问题
        WARNING：发生很重要的时间，但并不时错误时，如用户登录密码错误
        INFO：处理请求或者状态变化等日常事务
        DEBUG：调试过程中使用DEBUG等级，如算法中的每个循环的中间状态
        ！！若等级为info，则debug不会输出
    logging的使用 ：logging.basiceConfig(level=?,format = ?) 默认输出到控制台
        level:设置输出的级别
        format：输出格式参数：%(asctime)s:系统内时间
                            %(name)s :信息源名称
4、日志写入到文件（默认输出控制台
    1）创建并设置logger对象
    2）创建并设置FileHandler对象
    3）logger对象添加到FileHandler对象，并测试输出

5、既写入文件又写入控制台

6、Requests模块
    pip install -U Requests
    使用前导入 import
    可以用GET请求方式向服务器发送请求并获取服务器响应的数据集对象

7、XPath解析